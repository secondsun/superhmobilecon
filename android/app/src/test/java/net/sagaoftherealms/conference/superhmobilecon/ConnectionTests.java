package net.sagaoftherealms.conference.superhmobilecon;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * This class tests that the correct conference events and sent and handled by Android
 */
public class ConnectionTests {
    @Test
    /*
    * A user will, at some point, sign into the application.
    *
    * The sign in mechanism needs to emit an event that lets the applicaiton know it can move into
    * a signed in state.
    */
    public void testSignIn() {
        throw new IllegalStateException("Not implemented");
    }
}